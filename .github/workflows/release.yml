name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: read

jobs:
  release-windows-x64:
    runs-on: windows-2025
    permissions:
      contents: write
      id-token: write
      attestations: write

    steps:
      - name: Check out tagged source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Validate public surface
        shell: pwsh
        run: ./tools/check-public-surface.ps1

      - name: Validate tag, version resources, and changelog
        shell: pwsh
        run: ./tools/prepare-release.ps1 -Tag '${{ github.ref_name }}' -ValidateOnly

      - name: Configure (Visual Studio 2022)
        shell: pwsh
        run: cmake -S . -B out/build/release-ci -G "Visual Studio 17 2022" -A x64 -DBUILD_TESTING=ON

      - name: Build
        shell: pwsh
        run: cmake --build out/build/release-ci --config Release --parallel

      - name: Test
        shell: pwsh
        run: ctest --test-dir out/build/release-ci -C Release --output-on-failure

      - name: Authenticode sign and verify
        shell: pwsh
        env:
          WINDOWS_CERTIFICATE_BASE64: ${{ secrets.WINDOWS_CERTIFICATE_BASE64 }}
          WINDOWS_CERTIFICATE_PASSWORD: ${{ secrets.WINDOWS_CERTIFICATE_PASSWORD }}
        run: |
          if ([string]::IsNullOrWhiteSpace($env:WINDOWS_CERTIFICATE_BASE64) -or
              [string]::IsNullOrWhiteSpace($env:WINDOWS_CERTIFICATE_PASSWORD)) {
            throw 'Release signing secrets are not configured; unsigned releases are blocked.'
          }
          $pfx = Join-Path $env:RUNNER_TEMP 'release-signing.pfx'
          try {
            [IO.File]::WriteAllBytes($pfx, [Convert]::FromBase64String($env:WINDOWS_CERTIFICATE_BASE64))
            $signTool = Get-ChildItem "${env:ProgramFiles(x86)}\Windows Kits\10\bin\*\x64\signtool.exe" |
              Sort-Object FullName -Descending | Select-Object -First 1
            if ($null -eq $signTool) { throw 'signtool.exe was not found on the runner.' }
            foreach ($file in @(
              'out/build/release-ci/Release/GpuThermalGuard.exe',
              'out/build/release-ci/Release/GpuThermalGuardProbe.exe'
            )) {
              & $signTool.FullName sign /fd SHA256 /tr http://timestamp.digicert.com /td SHA256 /f $pfx /p $env:WINDOWS_CERTIFICATE_PASSWORD $file
              if ($LASTEXITCODE -ne 0) { throw "Signing failed for $file." }
              & $signTool.FullName verify /pa /v $file
              if ($LASTEXITCODE -ne 0) { throw "Signature verification failed for $file." }
            }
          } finally {
            Remove-Item -LiteralPath $pfx -Force -ErrorAction SilentlyContinue
          }

      - name: Prepare package, checksums, and release notes
        shell: pwsh
        run: ./tools/prepare-release.ps1 -Tag '${{ github.ref_name }}'

      - name: Attest release artifacts
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: |
            dist/*.zip
            dist/SHA256SUMS.txt

      - name: Create GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = '${{ github.ref_name }}'
          $arguments = @(
            'release', 'create', $tag,
            '--verify-tag',
            '--title', "GpuThermalGuard $tag",
            '--notes-file', 'dist/release-notes.md',
            ('dist/GpuThermalGuard-' + $tag.Substring(1) + '-windows-x64.zip'),
            'dist/SHA256SUMS.txt'
          )
          if ($tag.Contains('-')) { $arguments += '--prerelease' }
          & gh @arguments
          if ($LASTEXITCODE -ne 0) { throw 'GitHub Release creation failed.' }
